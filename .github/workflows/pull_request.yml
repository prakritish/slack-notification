name: Pull Request

on:
    pull_request:
        branches: ['main']

jobs:
    build:
        name: Build
        runs-on: ['ubuntu-latest']
        steps:
            - name: Checkout
              uses: actions/checkout@v4
            - name: Install packages
              run: |
                yarn
            - name: Build
              run: |
                yarn build
        